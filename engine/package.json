{
  "name": "@routeready/scheduling-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic, explainable, rule-based driver-to-shift scheduling engine.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test --experimental-strip-types \"test/*.test.ts\"",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^6.0.0"
  }
}
