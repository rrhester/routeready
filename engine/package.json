{
  "name": "@routeready/scheduling-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic, explainable, rule-based driver-to-shift scheduling engine.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test --experimental-strip-types \"test/*.test.ts\"",
    "check": "npm run typecheck && npm test",
    "build:dashboard": "esbuild src/browser-entry.ts --bundle --format=esm --target=es2022 --outfile=../dashboard/scheduling-engine.js --banner:js=\"// GENERATED by engine/ — do not edit. Run: cd engine && npm run build:dashboard\""
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "esbuild": "0.28.0",
    "typescript": "^6.0.0"
  }
}
